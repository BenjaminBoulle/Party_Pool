<%= simple_form_for @location do |f| %>
  <%= f.input :title %>
  <%= f.input :address %>
  <%= f.input :description %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>

  <%= f.button :submit %>
<% end %>
